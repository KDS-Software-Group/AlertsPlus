<Window x:Class="Noticore.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification" Height="100" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">

    <Border CornerRadius="15" Background="#222222" BorderBrush="#444444" BorderThickness="1">
        <Grid>
            <!-- Close Button -->
            <Button Content="✕" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top" 
                    Background="Transparent" 
                    Foreground="Gray" 
                    BorderThickness="0" 
                    Margin="8,5"
                    Cursor="Hand"
                    Click="CloseBtn_Click"/>

            <!-- Content Area -->
            <StackPanel Margin="15" VerticalAlignment="Center">
                <TextBlock x:Name="TitleTxt" 
                           Text="System Alert" 
                           Foreground="White" 
                           FontWeight="Bold" 
                           FontSize="14"/>
                <TextBlock x:Name="MessageTxt" 
                           Text="Heat Warning" 
                           Foreground="Silver" 
                           FontSize="12" 
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>