<UserControl x:Class="Noticore.ViewScheduler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Noticore"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#1a1a1c"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,10,10"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#2C2C2E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Background" Value="#2C2C2E"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border Background="#2C2C2E" CornerRadius="8" Padding="5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox" Grid.Column="0" Foreground="White" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center"/>
                                <Button x:Name="PART_Button" Grid.Column="1" Background="Transparent" BorderThickness="0" Content="📅" Foreground="#0A84FF" Cursor="Hand"/>
                                <Popup x:Name="PART_Popup" Placement="Bottom" StaysOpen="False" AllowsTransparency="True"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="250" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="Notifications" Foreground="White" FontSize="26" FontWeight="Bold" Margin="0,0,0,15"/>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="NotificationsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}">
                                <DockPanel>
                                    <Button Click="DeleteNotification_Click" Tag="{Binding Id}" DockPanel.Dock="Right" VerticalAlignment="Center">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="DelBox" Width="24" Height="24" CornerRadius="12" Background="Transparent">
                                                    <TextBlock x:Name="DelText" Text="✕" Foreground="#444447" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="DelBox" Property="Background" Value="#30FF453A"/>
                                                        <Setter TargetName="DelText" Property="Foreground" Value="#FF453A"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" Foreground="White" FontWeight="SemiBold" FontSize="15"/>
                                        <TextBlock Text="{Binding TimeDisplay}" Foreground="#0A84FF" FontSize="12" FontWeight="Bold"/>
                                    </StackPanel>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Border Grid.Column="2" Background="#1C1C1E" CornerRadius="15" Padding="18" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Quick Add" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,20"/>
                <TextBlock Text="NAME" Foreground="#636366" FontSize="9" FontWeight="Bold" Margin="2,0,0,5"/>
                <TextBox x:Name="TitleInput" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,15"/>
                <TextBlock Text="DATE" Foreground="#636366" FontSize="9" FontWeight="Bold" Margin="2,0,0,5"/>
                <DatePicker x:Name="DatePicker" Margin="0,0,0,15" />
                <TextBlock Text="TIME" Foreground="#636366" FontSize="9" FontWeight="Bold" Margin="2,0,0,5"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="HourInput" Text="12" Style="{StaticResource ModernTextBoxStyle}" TextAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="1" x:Name="MinuteInput" Text="00" Style="{StaticResource ModernTextBoxStyle}" TextAlignment="Center" Margin="0,0,5,0"/>
                    <ComboBox Grid.Column="2" x:Name="AmPmInput" SelectedIndex="0">
                        <ComboBoxItem Content="AM"/>
                        <ComboBoxItem Content="PM"/>
                    </ComboBox>
                </Grid>
                <Button Content="Schedule" Height="38" Background="#0A84FF" Foreground="White" FontWeight="Bold" Click="Schedule_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>