<UserControl x:Class="AlertsPlus.ViewScheduler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AlertsPlus"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#505050"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>


        <!-- problematic style for TextBox and ComboBox, any issues look here future me or devs please! -->
        <Style x:Key="ModernInput" TargetType="TextBox">
            <Setter Property="Background" Value="#222224"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernCombo" TargetType="ComboBox">
            <Setter Property="Background" Value="#222224"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"/>
                                <TextBlock Text="▾" Foreground="#505050" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ToggleButton Focusable="False"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent" BorderThickness="0" Opacity="0"/>
                                <Popup x:Name="Popup" Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="#222224" BorderBrush="#2d2d30" BorderThickness="1"
                                            CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="bg" Property="BorderBrush" Value="#3b82f6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2d2d30"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#2563eb"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1d4ed8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background" Value="#222224"/>
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#2a2a2c"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid Margin="20,16" x:Name="MainView">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Scheduled" Foreground="White" FontSize="18"
                           FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Content="+ New Reminder" HorizontalAlignment="Right"
                        Padding="16,8" Style="{StaticResource PrimaryBtn}"
                        Click="OpenForm_Click"/>
            </Grid>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                <ItemsControl x:Name="NotificationsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#1a1a1c" CornerRadius="10"
                                    BorderBrush="#2d2d30" BorderThickness="1"
                                    Padding="16,14" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Width="3" CornerRadius="2"
                                            Background="#3b82f6" Margin="0,0,14,0"/>

                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" Foreground="White"
                                                   FontWeight="SemiBold" FontSize="14"/>
                                        <TextBlock Text="{Binding Description}" Foreground="#606060"
                                                   FontSize="12" Margin="0,3,0,0"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding HasDescription}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                            <TextBlock Text="{Binding DateDisplay}" Foreground="#505050" FontSize="11"/>
                                            <TextBlock Text=" · " Foreground="#404040" FontSize="11"/>
                                            <TextBlock Text="{Binding TimeDisplay}" Foreground="#3b82f6"
                                                       FontSize="11" FontWeight="SemiBold"/>
                                            <TextBlock Text=" · " Foreground="#404040" FontSize="11"
                                                       Visibility="{Binding HasExe}"/>
                                            <TextBlock Text="🚀 App launch" Foreground="#505050" FontSize="11"
                                                       Visibility="{Binding HasExe}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <Button Grid.Column="2" Click="DeleteNotification_Click"
                                            Tag="{Binding Id}" VerticalAlignment="Center">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="DelBox" Width="28" Height="28"
                                                        CornerRadius="14" Background="Transparent">
                                                    <TextBlock x:Name="DelText" Text="✕"
                                                               Foreground="#404040"
                                                               VerticalAlignment="Center"
                                                               HorizontalAlignment="Center"
                                                               FontSize="11"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="DelBox" Property="Background" Value="#30FF453A"/>
                                                        <Setter TargetName="DelText" Property="Foreground" Value="#FF453A"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <Grid x:Name="ModalOverlay" Visibility="Collapsed">
            <Border Background="#88000000" IsHitTestVisible="False"/>
            <Grid MouseDown="CloseForm_Click" Background="Transparent"/>

            <Border Background="#1a1a1c" CornerRadius="14"
                    BorderBrush="#2d2d30" BorderThickness="1"
                    Padding="24" MaxWidth="420" MaxHeight="680"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    MouseDown="FormCard_MouseDown">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,16">
                        <TextBlock Text="New Reminder" Foreground="White"
                                   FontSize="16" FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                                Click="CloseForm_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="x" Width="28" Height="28" CornerRadius="14"
                                            Background="Transparent">
                                        <TextBlock x:Name="xt" Text="✕" Foreground="#505050"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center" FontSize="11"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="x" Property="Background" Value="#30FF453A"/>
                                            <Setter TargetName="xt" Property="Foreground" Value="#FF453A"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                        <StackPanel>

                            <TextBlock Text="TITLE" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="TitleInput"
                                     Style="{StaticResource ModernInput}"
                                     VerticalContentAlignment="Center"
                                     Height="44" Margin="0,0,0,14"/>

                            <TextBlock Text="DESCRIPTION" Style="{StaticResource FieldLabel}"/>
                            <TextBox x:Name="DescriptionInput"
                                     Style="{StaticResource ModernInput}"
                                     Height="80" TextWrapping="Wrap" AcceptsReturn="True"
                                     VerticalContentAlignment="Top"
                                     Padding="12,10"
                                     Margin="0,0,0,14"/>

                            <TextBlock Text="DATE" Style="{StaticResource FieldLabel}"/>
                            <DatePicker x:Name="DatePickerHidden"
                                        Height="44" Margin="0,0,0,14"
                                        SelectedDateChanged="DatePicker_SelectedDateChanged"
                                        Foreground="White" FontSize="13"
                                        Background="#222224">
                                <DatePicker.Resources>
                                    <Style TargetType="DatePickerTextBox">
                                        <Setter Property="Background" Value="#222224"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="CaretBrush" Value="White"/>
                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="DatePickerTextBox">
                                                    <Border Background="#222224" CornerRadius="8"
                                                            BorderBrush="#2d2d30" BorderThickness="1"
                                                            Padding="12,0">
                                                        <ScrollViewer x:Name="PART_ContentHost"
                                                                      VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </DatePicker.Resources>
                            </DatePicker>

                            <TextBlock Text="TIME" Style="{StaticResource FieldLabel}"/>
                            <Grid Margin="0,0,0,14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="HourInput" Text="12"
                                         Style="{StaticResource ModernInput}"
                                         VerticalContentAlignment="Center"
                                         TextAlignment="Center"
                                         Height="44" Grid.Column="0"/>
                                <TextBlock Text=":" Foreground="#505050" Grid.Column="1"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                           FontSize="18"/>
                                <TextBox x:Name="MinuteInput" Text="00"
                                         Style="{StaticResource ModernInput}"
                                         VerticalContentAlignment="Center"
                                         TextAlignment="Center"
                                         Height="44" Grid.Column="2"/>
                                <ComboBox x:Name="AmPmInput" SelectedIndex="0" Grid.Column="4"
                                          Style="{StaticResource ModernCombo}" Height="44">
                                    <ComboBoxItem Content="AM"/>
                                    <ComboBoxItem Content="PM"/>
                                </ComboBox>
                            </Grid>

                            <TextBlock Text="LAUNCH APP (OPTIONAL)" Style="{StaticResource FieldLabel}"/>
                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="ExePathInput"
                                         Style="{StaticResource ModernInput}"
                                         VerticalContentAlignment="Center"
                                         IsReadOnly="True" Height="44" Grid.Column="0"
                                         Foreground="#606060"/>
                                <Button Grid.Column="2" Content="Browse" Padding="14,10"
                                        Style="{StaticResource GhostBtn}"
                                        Click="BrowseExe_Click"/>
                            </Grid>

                        </StackPanel>
                    </ScrollViewer>

                    <Button Grid.Row="2" Content="Schedule Reminder" Height="42"
                            Margin="0,16,0,0" Padding="0"
                            Style="{StaticResource PrimaryBtn}"
                            Click="Schedule_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>