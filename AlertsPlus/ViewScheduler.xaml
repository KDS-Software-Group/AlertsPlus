<UserControl x:Class="AlertPlus.ViewScheduler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:AlertPlus"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#505050"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="ModernInput" TargetType="TextBox">
            <Setter Property="Background" Value="#1a1a1c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="bg" Property="BorderBrush" Value="#3b82f6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernDatePicker" TargetType="DatePicker">
            <Setter Property="Background" Value="#1a1a1c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <DatePickerTextBox x:Name="PART_TextBox"
                                                   Grid.Column="0"
                                                   Background="Transparent"
                                                   Foreground="White"
                                                   BorderThickness="0"
                                                   Padding="12,10"
                                                   VerticalContentAlignment="Center"
                                                   FontSize="13"/>
                                <Button x:Name="PART_Button" Grid.Column="1"
                                        Focusable="False" Width="32"
                                        Background="Transparent" BorderThickness="0"
                                        VerticalAlignment="Stretch">
                                    <TextBlock Text="📅" FontSize="13" VerticalAlignment="Center"/>
                                </Button>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True"
                                       Placement="Bottom" StaysOpen="False"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="bg" Property="BorderBrush" Value="#3b82f6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCombo" TargetType="ComboBox">
            <Setter Property="Background" Value="#1a1a1c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center"/>
                                <TextBlock Text="▾" Foreground="#505050" HorizontalAlignment="Right"
                                           VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ToggleButton Focusable="False" IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                              Background="Transparent" BorderThickness="0" Opacity="0"/>
                                <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="#1a1a1c" BorderBrush="#2d2d30" BorderThickness="1"
                                            CornerRadius="8" MinWidth="{TemplateBinding ActualWidth}">
                                        <ScrollViewer>
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="bg" Property="BorderBrush" Value="#3b82f6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Resources>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#2d2d30"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#3b82f6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#2563eb"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#1d4ed8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background" Value="#1a1a1c"/>
            <Setter Property="Foreground" Value="#808080"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2d2d30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#222224"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <Grid Margin="20,16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="280" MinWidth="260"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT: Notification List -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Scheduled" Foreground="White" FontSize="18"
                       FontWeight="SemiBold" Margin="0,0,0,16"/>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                <ItemsControl x:Name="NotificationsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#1a1a1c" CornerRadius="10"
                                    BorderBrush="#2d2d30" BorderThickness="1"
                                    Padding="16,14" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Blue accent bar -->
                                    <Border Grid.Column="0" Width="3" CornerRadius="2"
                                            Background="#3b82f6" Margin="0,0,14,0"/>

                                    <!-- Content -->
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" Foreground="White"
                                                   FontWeight="SemiBold" FontSize="14"/>
                                        <TextBlock Text="{Binding Description}" Foreground="#606060"
                                                   FontSize="12" Margin="0,3,0,0"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding HasDescription}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                            <TextBlock Text="{Binding DateDisplay}" Foreground="#505050" FontSize="11"/>
                                            <TextBlock Text=" · " Foreground="#404040" FontSize="11"/>
                                            <TextBlock Text="{Binding TimeDisplay}" Foreground="#3b82f6" FontSize="11" FontWeight="SemiBold"/>
                                            <TextBlock Text=" · " Foreground="#404040" FontSize="11"
                                                       Visibility="{Binding HasExe}"/>
                                            <TextBlock Text="🚀 App launch" Foreground="#505050" FontSize="11"
                                                       Visibility="{Binding HasExe}"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Delete -->
                                    <Button Grid.Column="2" Click="DeleteNotification_Click"
                                            Tag="{Binding Id}" VerticalAlignment="Center">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="DelBox" Width="28" Height="28"
                                                        CornerRadius="14" Background="Transparent">
                                                    <TextBlock x:Name="DelText" Text="✕"
                                                               Foreground="#404040"
                                                               VerticalAlignment="Center"
                                                               HorizontalAlignment="Center"
                                                               FontSize="11"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="DelBox" Property="Background" Value="#30FF453A"/>
                                                        <Setter TargetName="DelText" Property="Foreground" Value="#FF453A"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- RIGHT: Add Panel -->
        <Border Grid.Column="2" Background="#1a1a1c" CornerRadius="12"
                BorderBrush="#2d2d30" BorderThickness="1"
                Padding="20" VerticalAlignment="Top">
            <StackPanel>

                <TextBlock Text="New Reminder" Foreground="White" FontSize="15"
                           FontWeight="SemiBold" Margin="0,0,0,20"/>

                <!-- TITLE -->
                <TextBlock Text="TITLE" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="TitleInput" Style="{StaticResource ModernInput}" Margin="0,0,0,14"/>

                <!-- DESCRIPTION -->
                <TextBlock Text="DESCRIPTION" Style="{StaticResource FieldLabel}"/>
                <TextBox x:Name="DescriptionInput" Style="{StaticResource ModernInput}"
                         Height="70" TextWrapping="Wrap" AcceptsReturn="True"
                         VerticalContentAlignment="Top" Margin="0,0,0,14"/>

                <!-- DATE -->
                <TextBlock Text="DATE" Style="{StaticResource FieldLabel}"/>
                <DatePicker x:Name="DatePicker" Style="{StaticResource ModernDatePicker}"
                            Margin="0,0,0,14"/>

                <!-- TIME -->
                <TextBlock Text="TIME" Style="{StaticResource FieldLabel}"/>
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="HourInput" Text="12" Style="{StaticResource ModernInput}"
                             TextAlignment="Center" Grid.Column="0"/>
                    <TextBlock Text=":" Foreground="#505050" Grid.Column="1"
                               HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                    <TextBox x:Name="MinuteInput" Text="00" Style="{StaticResource ModernInput}"
                             TextAlignment="Center" Grid.Column="2"/>
                    <ComboBox x:Name="AmPmInput" SelectedIndex="0" Grid.Column="4"
                              Style="{StaticResource ModernCombo}">
                        <ComboBoxItem Content="AM"/>
                        <ComboBoxItem Content="PM"/>
                    </ComboBox>
                </Grid>

                <!-- LAUNCH APP -->
                <TextBlock Text="LAUNCH APP (OPTIONAL)" Style="{StaticResource FieldLabel}"/>
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="8"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="ExePathInput" Style="{StaticResource ModernInput}"
                             IsReadOnly="True" Grid.Column="0"
                             Foreground="#606060"/>
                    <Button Grid.Column="2" Content="Browse" Padding="14,10"
                            Style="{StaticResource GhostBtn}"
                            Click="BrowseExe_Click"/>
                </Grid>

                <!-- SUBMIT -->
                <Button Content="Schedule Reminder" Height="40" Padding="0"
                        Style="{StaticResource PrimaryBtn}"
                        Click="Schedule_Click"/>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>